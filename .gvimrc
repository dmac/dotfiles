if has('unix')
  set guifont=Inconsolata\ for\ Powerline\ 12
else
  set guifont=Inconsolata\ for\ Powerline:h16
endif
set guioptions-=T                            " hide gui toolbar
set guioptions-=m                            " hide gui menu
set novisualbell                             " be quiet
